### 首先是对于src/core/types的构造
- 这个文件定义了整个项目的"语言规范",方便未来的引用
```pyhton
# 图的原始数据结构 (Adjacency List)# 格式: { "StartNode": [("TargetNode", Cost), ...], ... } 这里以后可能还是要注意的  
GraphData = Dict[NodeID, List[Tuple[NodeID, Cost]]]
```
这个统一了路线的格式,感觉还要研究一下,先放着
